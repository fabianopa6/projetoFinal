<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation" style="background-color: #047; border-color: #047;">

  <!--<div class="col-md-1">
    <%= image_tag("fapicon2.png")%>
  </div>
-->

  <div class="container">

    <div class="navbar-header">
      <b><%= link_to "CodeFAP", root_path, class: "navbar-brand"%></b>
    </div>

    <div class="col-md-6 col-md-offset-1">
        <ul class="nav navbar-nav">
          <% if user_signed_in? %>
            <li><%= link_to "Nova Disciplina ", new_subject_path %></li>

              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"> Visualizar por:
                  <span class="caret"></span></a>
                <ul class="dropdown-menu" role="menu">
                  <% Semester.all.each do |semester| %>
                    <li class="<%= 'active' if params[:semester] == semester.semester %>">
                      <%= link_to semester.semester, subjects_path(semester: semester.semester), class:"link"%>
                    </li>
                  <% end %>
                </ul>
              </li>
          <% end %>
        </ul>
    </div>



    <ul class="nav navbar-nav navbar-right">

    	
      <% if user_signed_in? %>

    		<li><%= link_to "|  Sair", destroy_user_session_path, method: :delete %></li>
    	<% else %>
    		<li><%= link_to "Entrar  |", new_user_session_path %></li>
          <li><%= link_to "Cadastre-se", new_user_registration_path %></li>
    	<% end %>
    </ul>

  </div>
</nav>